// use url::Url;
//use dirs::download_dir;
//use std::path::PathBuf;

/*pub fn valider_url(url: &str) -> Result<Url, &'static str> {
    Url::parse(url).map_err(|_| "URL invalide")
}

pub fn obtenir_chemin_telechargement(nom_fichier: &str) -> Option<PathBuf> {
    download_dir().map(|dir| dir.join(nom_fichier))
}*/
